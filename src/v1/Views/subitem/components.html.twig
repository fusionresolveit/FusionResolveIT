{{ include('header.html.twig') }}
{{ include('headerForm.html.twig') }}



<div class="ui stackable cards">
  {% for firmware in data.firmwares|sort((a, b) => a.firmware.name|lower <=> b.firmware.name|lower) %}
    <div class="ui {{ firmware.color }} card">
      <div class="content">
        <div class="header">{{ firmware.name }}</div>
        <div class="meta"><span class="ui {{ firmware.color }} text">{{ translation.firmware }}</span></div>
      </div>
      <div class="content">
        <div class="ui relaxed list">
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.manufacturer }}</a>
              <div class="description">
                {% if firmware.manufacturer_url != '' %}
                    <a href="{{ firmware.manufacturer_url }}">{{ firmware.manufacturer }}</a>
                {% else %}
                    {{ firmware.manufacturer }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.type }}</a>
              <div class="description">
                {% if firmware.type_url != '' %}
                    <a href="{{ firmware.type_url }}">{{ firmware.type }}</a>
                {% else %}
                    {{ firmware.type }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.version }}</a>
              <div class="description">{{ firmware.version }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.install_date }}</a>
              <div class="description">{{ firmware.date }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.serial }}</a>
              <div class="description">{{ firmware.serial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.inventaire_number }}</a>
              <div class="description">{{ firmware.otherserial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.location }}</a>
              <div class="description">
                {% if firmware.location_url != '' %}
                    <a href="{{ firmware.location_url }}">{{ firmware.location }}</a>
                {% else %}
                    {{ firmware.location }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.status }}</a>
              <div class="description">
                {% if firmware.state_url != '' %}
                    <a href="{{ firmware.state_url }}">{{ firmware.state }}</a>
                {% else %}
                    {{ firmware.state }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.documents }}</a>
              {% for document in firmware.documents %}
                <div class="description">
                  {% if document.url != '' %}
                      <a href="{{ document.url }}">{{ document.name }}</a>
                  {% else %}
                      {{ document.name }}
                  {% endif %}
                </div>
              {% endfor %}
            </div>
          </div>
        </div>
      </div>
    </div>
  {% endfor %}

  {% for processor in data.processors|sort((a, b) => a.processor.name|lower <=> b.processor.name|lower) %}
    <div class="ui {{ processor.color }} card">
      <div class="content">
        <div class="header">{{ processor.name }}</div>
        <div class="meta"><span class="ui {{ processor.color }} text">{{ translation.processor }}</span></div>
      </div>
      <div class="content">
        <div class="ui relaxed list">
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.manufacturer }}</a>
              <div class="description">
                {% if processor.manufacturer_url != '' %}
                    <a href="{{ processor.manufacturer_url }}">{{ processor.manufacturer }}</a>
                {% else %}
                    {{ processor.manufacturer }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.frequence_mhz }}</a>
              <div class="description">{{ processor.frequency }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.serial }}</a>
              <div class="description">{{ processor.serial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.inventaire_number }}</a>
              <div class="description">{{ processor.otherserial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.location }}</a>
              <div class="description">
                {% if processor.location_url != '' %}
                    <a href="{{ processor.location_url }}">{{ processor.location }}</a>
                {% else %}
                    {{ processor.location }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.status }}</a>
              <div class="description">
                {% if processor.state_url != '' %}
                    <a href="{{ processor.state_url }}">{{ processor.state }}</a>
                {% else %}
                    {{ processor.state }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.nbcores }}</a>
              <div class="description">{{ processor.nbcores }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.nbthreads }}</a>
              <div class="description">{{ processor.nbthreads }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.busID }}</a>
              <div class="description">{{ processor.busID }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.documents }}</a>
              {% for document in processor.documents %}
                <div class="description">
                  {% if document.url != '' %}
                      <a href="{{ document.url }}">{{ document.name }}</a>
                  {% else %}
                      {{ document.name }}
                  {% endif %}
                </div>
              {% endfor %}
            </div>
          </div>
        </div>
      </div>
    </div>
  {% endfor %}

  {% for memory in data.memories|sort((a, b) => a.memory.name|lower <=> b.memory.name|lower) %}
    <div class="ui {{ memory.color }} card">
      <div class="content">
        <div class="header">{{ memory.name }}</div>
        <div class="meta"><span class="ui {{ memory.color }} text">{{ translation.memory }}</span></div>
      </div>
      <div class="content">
        <div class="ui relaxed list">
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.manufacturer }}</a>
              <div class="description">
                {% if memory.manufacturer_url != '' %}
                    <a href="{{ memory.manufacturer_url }}">{{ memory.manufacturer }}</a>
                {% else %}
                    {{ memory.manufacturer }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.type }}</a>
              <div class="description">
                {% if memory.type_url != '' %}
                    <a href="{{ memory.type_url }}">{{ memory.type }}</a>
                {% else %}
                    {{ memory.type }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.frequence }}</a>
              <div class="description">{{ memory.frequence }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.size }}</a>
              <div class="description">{{ memory.size }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.serial }}</a>
              <div class="description">{{ memory.serial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.inventaire_number }}</a>
              <div class="description">{{ memory.otherserial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.location }}</a>
              <div class="description">
                {% if memory.location_url != '' %}
                    <a href="{{ memory.location_url }}">{{ memory.location }}</a>
                {% else %}
                    {{ memory.location }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.status }}</a>
              <div class="description">
                {% if memory.state_url != '' %}
                    <a href="{{ memory.state_url }}">{{ memory.state }}</a>
                {% else %}
                    {{ memory.state }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.busID }}</a>
              <div class="description">{{ memory.busID }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.documents }}</a>
              {% for document in memory.documents %}
                <div class="description">
                  {% if document.url != '' %}
                      <a href="{{ document.url }}">{{ document.name }}</a>
                  {% else %}
                      {{ document.name }}
                  {% endif %}
                </div>
              {% endfor %}
            </div>
          </div>
        </div>
      </div>
    </div>
  {% endfor %}

  {% for harddrive in data.harddrives|sort((a, b) => a.harddrive.name|lower <=> b.harddrive.name|lower) %}
    <div class="ui {{ harddrive.color }} card">
      <div class="content">
        <div class="header">{{ harddrive.name }}</div>
        <div class="meta"><span class="ui {{ harddrive.color }} text">{{ translation.harddrive }}</span></div>
      </div>
      <div class="content">
        <div class="ui relaxed list">
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.manufacturer }}</a>
              <div class="description">
                {% if harddrive.manufacturer_url != '' %}
                    <a href="{{ harddrive.manufacturer_url }}">{{ harddrive.manufacturer }}</a>
                {% else %}
                    {{ harddrive.manufacturer }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.rpm }}</a>
              <div class="description">{{ harddrive.rpm }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.cache }}</a>
              <div class="description">{{ harddrive.cache }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.interface }}</a>
              <div class="description">
                {% if harddrive.interface_url != '' %}
                    <a href="{{ harddrive.interface_url }}">{{ harddrive.interface }}</a>
                {% else %}
                    {{ harddrive.interface }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.capacity }}</a>
              <div class="description">{{ harddrive.capacity }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.serial }}</a>
              <div class="description">{{ harddrive.serial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.inventaire_number }}</a>
              <div class="description">{{ harddrive.otherserial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.location }}</a>
              <div class="description">
                {% if harddrive.location_url != '' %}
                    <a href="{{ harddrive.location_url }}">{{ harddrive.location }}</a>
                {% else %}
                    {{ harddrive.location }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.status }}</a>
              <div class="description">
                {% if harddrive.state_url != '' %}
                    <a href="{{ harddrive.state_url }}">{{ harddrive.state }}</a>
                {% else %}
                    {{ harddrive.state }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.busID }}</a>
              <div class="description">{{ harddrive.busID }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.documents }}</a>
              {% for document in harddrive.documents %}
                <div class="description">
                  {% if document.url != '' %}
                      <a href="{{ document.url }}">{{ document.name }}</a>
                  {% else %}
                      {{ document.name }}
                  {% endif %}
                </div>
              {% endfor %}
            </div>
          </div>
        </div>
      </div>
    </div>
  {% endfor %}

  {% for battery in data.batteries|sort((a, b) => a.battery.name|lower <=> b.battery.name|lower) %}
    <div class="ui {{ battery.color }} card">
      <div class="content">
        <div class="header">{{ battery.name }}</div>
        <div class="meta"><span class="ui {{ battery.color }} text">{{ translation.battery }}</span></div>
      </div>
      <div class="content">
        <div class="ui relaxed list">
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.manufacturer }}</a>
              <div class="description">
                {% if battery.manufacturer_url != '' %}
                    <a href="{{ battery.manufacturer_url }}">{{ battery.manufacturer }}</a>
                {% else %}
                    {{ battery.manufacturer }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.type }}</a>
              <div class="description">
                {% if battery.type_url != '' %}
                    <a href="{{ battery.type_url }}">{{ battery.type }}</a>
                {% else %}
                    {{ battery.type }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.voltage_mv }}</a>
              <div class="description">{{ battery.voltage }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.capacity_mwh }}</a>
              <div class="description">{{ battery.capacity }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.serial }}</a>
              <div class="description">{{ battery.serial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.inventaire_number }}</a>
              <div class="description">{{ battery.otherserial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.location }}</a>
              <div class="description">
                {% if battery.location_url != '' %}
                    <a href="{{ battery.location_url }}">{{ battery.location }}</a>
                {% else %}
                    {{ battery.location }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.status }}</a>
              <div class="description">
                {% if battery.state_url != '' %}
                    <a href="{{ battery.state_url }}">{{ battery.state }}</a>
                {% else %}
                    {{ battery.state }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.manufacturing_date }}</a>
              <div class="description">{{ battery.manufacturing_date }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.documents }}</a>
              {% for document in battery.documents %}
                <div class="description">
                  {% if document.url != '' %}
                      <a href="{{ document.url }}">{{ document.name }}</a>
                  {% else %}
                      {{ document.name }}
                  {% endif %}
                </div>
              {% endfor %}
            </div>
          </div>
        </div>
      </div>
    </div>
  {% endfor %}

  {% for soundcard in data.soundcards|sort((a, b) => a.soundcard.name|lower <=> b.soundcard.name|lower) %}
    <div class="ui {{ soundcard.color }} card">
      <div class="content">
        <div class="header">{{ soundcard.name }}</div>
        <div class="meta"><span class="ui {{ soundcard.color }} text">{{ translation.soundcard }}</span></div>
      </div>
      <div class="content">
        <div class="ui relaxed list">
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.manufacturer }}</a>
              <div class="description">
                {% if soundcard.manufacturer_url != '' %}
                    <a href="{{ soundcard.manufacturer_url }}">{{ soundcard.manufacturer }}</a>
                {% else %}
                    {{ soundcard.manufacturer }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.type }}</a>
              <div class="description">{{ soundcard.type }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.serial }}</a>
              <div class="description">{{ soundcard.serial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.inventaire_number }}</a>
              <div class="description">{{ soundcard.otherserial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.location }}</a>
              <div class="description">
                {% if soundcard.location_url != '' %}
                    <a href="{{ soundcard.location_url }}">{{ soundcard.location }}</a>
                {% else %}
                    {{ soundcard.location }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.status }}</a>
              <div class="description">
                {% if soundcard.state_url != '' %}
                    <a href="{{ soundcard.state_url }}">{{ soundcard.state }}</a>
                {% else %}
                    {{ soundcard.state }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.busID }}</a>
              <div class="description">{{ soundcard.busID }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.documents }}</a>
              {% for document in soundcard.documents %}
                <div class="description">
                  {% if document.url != '' %}
                      <a href="{{ document.url }}">{{ document.name }}</a>
                  {% else %}
                      {{ document.name }}
                  {% endif %}
                </div>
              {% endfor %}
            </div>
          </div>
        </div>
      </div>
    </div>
  {% endfor %}

  {% for controller in data.controllers|sort((a, b) => a.controller.name|lower <=> b.controller.name|lower) %}
    <div class="ui {{ controller.color }} card">
      <div class="content">
        <div class="header">{{ controller.name }}</div>
        <div class="meta"><span class="ui {{ controller.color }} text">{{ translation.controller }}</span></div>
      </div>
      <div class="content">
        <div class="ui relaxed list">
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.manufacturer }}</a>
              <div class="description">
                {% if controller.manufacturer_url != '' %}
                    <a href="{{ controller.manufacturer_url }}">{{ controller.manufacturer }}</a>
                {% else %}
                    {{ controller.manufacturer }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.interface }}</a>
              <div class="description">
                {% if controller.interface_url != '' %}
                    <a href="{{ controller.interface_url }}">{{ controller.interface }}</a>
                {% else %}
                    {{ controller.interface }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.serial }}</a>
              <div class="description">{{ controller.serial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.inventaire_number }}</a>
              <div class="description">{{ controller.otherserial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.location }}</a>
              <div class="description">
                {% if controller.location_url != '' %}
                    <a href="{{ controller.location_url }}">{{ controller.location }}</a>
                {% else %}
                    {{ controller.location }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.status }}</a>
              <div class="description">
                {% if controller.state_url != '' %}
                    <a href="{{ controller.state_url }}">{{ controller.state }}</a>
                {% else %}
                    {{ controller.state }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.busID }}</a>
              <div class="description">{{ controller.busID }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.documents }}</a>
              {% for document in controller.documents %}
                <div class="description">
                  {% if document.url != '' %}
                      <a href="{{ document.url }}">{{ document.name }}</a>
                  {% else %}
                      {{ document.name }}
                  {% endif %}
                </div>
              {% endfor %}
            </div>
          </div>
        </div>
      </div>
    </div>
  {% endfor %}

  {% for powersupply in data.powersupplies|sort((a, b) => a.powersupply.name|lower <=> b.powersupply.name|lower) %}
    <div class="ui {{ powersupply.color }} card">
      <div class="content">
        <div class="header">{{ powersupply.name }}</div>
        <div class="meta"><span class="ui {{ powersupply.color }} text">{{ translation.powersupply }}</span></div>
      </div>
      <div class="content">
        <div class="ui relaxed list">
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.manufacturer }}</a>
              <div class="description">
                {% if powersupply.manufacturer_url != '' %}
                    <a href="{{ powersupply.manufacturer_url }}">{{ powersupply.manufacturer }}</a>
                {% else %}
                    {{ powersupply.manufacturer }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.serial }}</a>
              <div class="description">{{ powersupply.serial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.inventaire_number }}</a>
              <div class="description">{{ powersupply.otherserial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.location }}</a>
              <div class="description">
                {% if powersupply.location_url != '' %}
                    <a href="{{ powersupply.location_url }}">{{ powersupply.location }}</a>
                {% else %}
                    {{ powersupply.location }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.status }}</a>
              <div class="description">
                {% if powersupply.state_url != '' %}
                    <a href="{{ powersupply.state_url }}">{{ powersupply.state }}</a>
                {% else %}
                    {{ powersupply.state }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.documents }}</a>
              {% for document in powersupply.documents %}
                <div class="description">
                  {% if document.url != '' %}
                      <a href="{{ document.url }}">{{ document.name }}</a>
                  {% else %}
                      {{ document.name }}
                  {% endif %}
                </div>
              {% endfor %}
            </div>
          </div>
        </div>
      </div>
    </div>
  {% endfor %}

  {% for sensor in data.sensors|sort((a, b) => a.sensor.name|lower <=> b.sensor.name|lower) %}
    <div class="ui {{ sensor.color }} card">
      <div class="content">
        <div class="header">{{ sensor.name }}</div>
        <div class="meta"><span class="ui {{ sensor.color }} text">{{ translation.sensor }}</span></div>
      </div>
      <div class="content">
        <div class="ui relaxed list">
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.manufacturer }}</a>
              <div class="description">
                {% if sensor.manufacturer_url != '' %}
                    <a href="{{ sensor.manufacturer_url }}">{{ sensor.manufacturer }}</a>
                {% else %}
                    {{ sensor.manufacturer }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.serial }}</a>
              <div class="description">{{ sensor.serial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.inventaire_number }}</a>
              <div class="description">{{ sensor.otherserial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.location }}</a>
              <div class="description">
                {% if sensor.location_url != '' %}
                    <a href="{{ sensor.location_url }}">{{ sensor.location }}</a>
                {% else %}
                    {{ sensor.location }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.status }}</a>
              <div class="description">
                {% if sensor.state_url != '' %}
                    <a href="{{ sensor.state_url }}">{{ sensor.state }}</a>
                {% else %}
                    {{ sensor.state }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.documents }}</a>
              {% for document in sensor.documents %}
                <div class="description">
                  {% if document.url != '' %}
                      <a href="{{ document.url }}">{{ document.name }}</a>
                  {% else %}
                      {{ document.name }}
                  {% endif %}
                </div>
              {% endfor %}
            </div>
          </div>
        </div>
      </div>
    </div>
  {% endfor %}

  {% for devicepci in data.devicepcis|sort((a, b) => a.devicepci.name|lower <=> b.devicepci.name|lower) %}
    <div class="ui {{ devicepci.color }} card">
      <div class="content">
        <div class="header">{{ devicepci.name }}</div>
        <div class="meta"><span class="ui {{ devicepci.color }} text">{{ translation.devicepci }}</span></div>
      </div>
      <div class="content">
        <div class="ui relaxed list">
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.serial }}</a>
              <div class="description">{{ devicepci.serial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.inventaire_number }}</a>
              <div class="description">{{ devicepci.otherserial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.location }}</a>
              <div class="description">
                {% if devicepci.location_url != '' %}
                    <a href="{{ devicepci.location_url }}">{{ devicepci.location }}</a>
                {% else %}
                    {{ devicepci.location }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.status }}</a>
              <div class="description">
                {% if devicepci.state_url != '' %}
                    <a href="{{ devicepci.state_url }}">{{ devicepci.state }}</a>
                {% else %}
                    {{ devicepci.state }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.busID }}</a>
              <div class="description">{{ devicepci.busID }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.documents }}</a>
              {% for document in devicepci.documents %}
                <div class="description">
                  {% if document.url != '' %}
                      <a href="{{ document.url }}">{{ document.name }}</a>
                  {% else %}
                      {{ document.name }}
                  {% endif %}
                </div>
              {% endfor %}
            </div>
          </div>
        </div>
      </div>
    </div>
  {% endfor %}

  {% for devicegeneric in data.devicegenerics|sort((a, b) => a.devicegeneric.name|lower <=> b.devicegeneric.name|lower) %}
    <div class="ui {{ devicegeneric.color }} card">
      <div class="content">
        <div class="header">{{ devicegeneric.name }}</div>
        <div class="meta"><span class="ui {{ devicegeneric.color }} text">{{ translation.devicegeneric }}</span></div>
      </div>
      <div class="content">
        <div class="ui relaxed list">
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.manufacturer }}</a>
              <div class="description">
                {% if devicegeneric.manufacturer_url != '' %}
                    <a href="{{ devicegeneric.manufacturer_url }}">{{ devicegeneric.manufacturer }}</a>
                {% else %}
                    {{ devicegeneric.manufacturer }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.serial }}</a>
              <div class="description">{{ devicegeneric.serial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.inventaire_number }}</a>
              <div class="description">{{ devicegeneric.otherserial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.location }}</a>
              <div class="description">
                {% if devicegeneric.location_url != '' %}
                    <a href="{{ devicegeneric.location_url }}">{{ devicegeneric.location }}</a>
                {% else %}
                    {{ devicegeneric.location }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.status }}</a>
              <div class="description">
                {% if devicegeneric.state_url != '' %}
                    <a href="{{ devicegeneric.state_url }}">{{ devicegeneric.state }}</a>
                {% else %}
                    {{ devicegeneric.state }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.documents }}</a>
              {% for document in devicegeneric.documents %}
                <div class="description">
                  {% if document.url != '' %}
                      <a href="{{ document.url }}">{{ document.name }}</a>
                  {% else %}
                      {{ document.name }}
                  {% endif %}
                </div>
              {% endfor %}
            </div>
          </div>
        </div>
      </div>
    </div>
  {% endfor %}

  {% for devicenetworkcard in data.devicenetworkcards|sort((a, b) => a.devicenetworkcard.name|lower <=> b.devicenetworkcard.name|lower) %}
    <div class="ui {{ devicenetworkcard.color }} card">
      <div class="content">
        <div class="header">{{ devicenetworkcard.name }}</div>
        <div class="meta"><span class="ui {{ devicenetworkcard.color }} text">{{ translation.devicenetworkcard }}</span></div>
      </div>
      <div class="content">
        <div class="ui relaxed list">
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.manufacturer }}</a>
              <div class="description">
                {% if devicenetworkcard.manufacturer_url != '' %}
                    <a href="{{ devicenetworkcard.manufacturer_url }}">{{ devicenetworkcard.manufacturer }}</a>
                {% else %}
                    {{ devicenetworkcard.manufacturer }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.flow }}</a>
              <div class="description">{{ devicenetworkcard.speed }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.mac_address }}</a>
              <div class="description">{{ devicenetworkcard.mac_address }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.serial }}</a>
              <div class="description">{{ devicenetworkcard.serial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.inventaire_number }}</a>
              <div class="description">{{ devicenetworkcard.otherserial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.location }}</a>
              <div class="description">
                {% if devicenetworkcard.location_url != '' %}
                    <a href="{{ devicenetworkcard.location_url }}">{{ devicenetworkcard.location }}</a>
                {% else %}
                    {{ devicenetworkcard.location }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.status }}</a>
              <div class="description">
                {% if devicenetworkcard.state_url != '' %}
                    <a href="{{ devicenetworkcard.state_url }}">{{ devicenetworkcard.state }}</a>
                {% else %}
                    {{ devicenetworkcard.state }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.busID }}</a>
              <div class="description">{{ devicenetworkcard.busID }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.documents }}</a>
              {% for document in devicenetworkcard.documents %}
                <div class="description">
                  {% if document.url != '' %}
                      <a href="{{ document.url }}">{{ document.name }}</a>
                  {% else %}
                      {{ document.name }}
                  {% endif %}
                </div>
              {% endfor %}
            </div>
          </div>
        </div>
      </div>
    </div>
  {% endfor %}

  {% for devicesimcard in data.devicesimcards|sort((a, b) => a.devicesimcard.name|lower <=> b.devicesimcard.name|lower) %}
    <div class="ui {{ devicesimcard.color }} card">
      <div class="content">
        <div class="header">{{ devicesimcard.name }}</div>
        <div class="meta"><span class="ui {{ devicesimcard.color }} text">{{ translation.devicesimcard }}</span></div>
      </div>
      <div class="content">
        <div class="ui relaxed list">
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.serial }}</a>
              <div class="description">{{ devicesimcard.serial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.inventaire_number }}</a>
              <div class="description">{{ devicesimcard.otherserial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.location }}</a>
              <div class="description">
                {% if devicesimcard.location_url != '' %}
                    <a href="{{ devicesimcard.location_url }}">{{ devicesimcard.location }}</a>
                {% else %}
                    {{ devicesimcard.location }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.status }}</a>
              <div class="description">
                {% if devicesimcard.state_url != '' %}
                    <a href="{{ devicesimcard.state_url }}">{{ devicesimcard.state }}</a>
                {% else %}
                    {{ devicesimcard.state }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.line }}</a>
              <div class="description">
                {% if devicesimcard.line_url != '' %}
                    <a href="{{ devicesimcard.line_url }}">{{ devicesimcard.line }}</a>
                {% else %}
                    {{ devicesimcard.line }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.msin }}</a>
              <div class="description">{{ devicesimcard.msin }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.user }}</a>
              <div class="description">
                {% if devicesimcard.user_url != '' %}
                    <a href="{{ devicesimcard.user_url }}">{{ devicesimcard.user }}</a>
                {% else %}
                    {{ devicesimcard.user }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.group }}</a>
              <div class="description">
                {% if devicesimcard.group_url != '' %}
                    <a href="{{ devicesimcard.group_url }}">{{ devicesimcard.group }}</a>
                {% else %}
                    {{ devicesimcard.group }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.documents }}</a>
              {% for document in devicesimcard.documents %}
                <div class="description">
                  {% if document.url != '' %}
                      <a href="{{ document.url }}">{{ document.name }}</a>
                  {% else %}
                      {{ document.name }}
                  {% endif %}
                </div>
              {% endfor %}
            </div>
          </div>
        </div>
      </div>
    </div>
  {% endfor %}

  {% for devicemotherboard in data.devicemotherboards|sort((a, b) => a.devicemotherboard.name|lower <=> b.devicemotherboard.name|lower) %}
    <div class="ui {{ devicemotherboard.color }} card">
      <div class="content">
        <div class="header">{{ devicemotherboard.name }}</div>
        <div class="meta"><span class="ui {{ devicemotherboard.color }} text">{{ translation.devicemotherboard }}</span></div>
      </div>
      <div class="content">
        <div class="ui relaxed list">
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.manufacturer }}</a>
              <div class="description">
                {% if devicemotherboard.manufacturer_url != '' %}
                    <a href="{{ devicemotherboard.manufacturer_url }}">{{ devicemotherboard.manufacturer }}</a>
                {% else %}
                    {{ devicemotherboard.manufacturer }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.serial }}</a>
              <div class="description">{{ devicemotherboard.serial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.inventaire_number }}</a>
              <div class="description">{{ devicemotherboard.otherserial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.location }}</a>
              <div class="description">
                {% if devicemotherboard.location_url != '' %}
                    <a href="{{ devicemotherboard.location_url }}">{{ devicemotherboard.location }}</a>
                {% else %}
                    {{ devicemotherboard.location }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.status }}</a>
              <div class="description">
                {% if devicemotherboard.state_url != '' %}
                    <a href="{{ devicemotherboard.state_url }}">{{ devicemotherboard.state }}</a>
                {% else %}
                    {{ devicemotherboard.state }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.documents }}</a>
              {% for document in devicemotherboard.documents %}
                <div class="description">
                  {% if document.url != '' %}
                      <a href="{{ document.url }}">{{ document.name }}</a>
                  {% else %}
                      {{ document.name }}
                  {% endif %}
                </div>
              {% endfor %}
            </div>
          </div>
        </div>
      </div>
    </div>
  {% endfor %}

  {% for devicecase in data.devicecases|sort((a, b) => a.devicecase.name|lower <=> b.devicecase.name|lower) %}
    <div class="ui {{ devicecase.color }} card">
      <div class="content">
        <div class="header">{{ devicecase.name }}</div>
        <div class="meta"><span class="ui {{ devicecase.color }} text">{{ translation.devicecase }}</span></div>
      </div>
      <div class="content">
        <div class="ui relaxed list">
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.manufacturer }}</a>
              <div class="description">
                {% if devicecase.manufacturer_url != '' %}
                    <a href="{{ devicecase.manufacturer_url }}">{{ devicecase.manufacturer }}</a>
                {% else %}
                    {{ devicecase.manufacturer }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.serial }}</a>
              <div class="description">{{ devicecase.serial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.inventaire_number }}</a>
              <div class="description">{{ devicecase.otherserial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.location }}</a>
              <div class="description">
                {% if devicecase.location_url != '' %}
                    <a href="{{ devicecase.location_url }}">{{ devicecase.location }}</a>
                {% else %}
                    {{ devicecase.location }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.status }}</a>
              <div class="description">
                {% if devicecase.state_url != '' %}
                    <a href="{{ devicecase.state_url }}">{{ devicecase.state }}</a>
                {% else %}
                    {{ devicecase.state }}
                {% endif %}
              </div>
            </div>
          </div>
          {% if devicecase.documents|length > 0%}
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.documents }}</a>
              {% for document in devicecase.documents %}
                <div class="description">
                  {% if document.url != '' %}
                      <a href="{{ document.url }}">{{ document.name }}</a>
                  {% else %}
                      {{ document.name }}
                  {% endif %}
                </div>
              {% endfor %}
            </div>
          </div>
          {% endif %}
        </div>
      </div>
    </div>
  {% endfor %}

  {% for devicegraphiccard in data.devicegraphiccards|sort((a, b) => a.devicegraphiccard.name|lower <=> b.devicegraphiccard.name|lower) %}
    <div class="ui {{ devicegraphiccard.color }} card">
      <div class="content">
        <div class="header">{{ devicegraphiccard.name }}</div>
        <div class="meta"><span class="ui {{ devicegraphiccard.color }} text">{{ translation.devicegraphiccard }}</span></div>
      </div>
      <div class="content">
        <div class="ui relaxed list">
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.manufacturer }}</a>
              <div class="description">
                {% if devicegraphiccard.manufacturer_url != '' %}
                    <a href="{{ devicegraphiccard.manufacturer_url }}">{{ devicegraphiccard.manufacturer }}</a>
                {% else %}
                    {{ devicegraphiccard.manufacturer }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.interface }}</a>
              <div class="description">
                {% if devicegraphiccard.interface_url != '' %}
                    <a href="{{ devicegraphiccard.interface_url }}">{{ devicegraphiccard.interface }}</a>
                {% else %}
                    {{ devicegraphiccard.interface }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.chipset }}</a>
              <div class="description">{{ devicegraphiccard.chipset }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.memory_mio }}</a>
              <div class="description">{{ devicegraphiccard.memory }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.serial }}</a>
              <div class="description">{{ devicegraphiccard.serial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.inventaire_number }}</a>
              <div class="description">{{ devicegraphiccard.otherserial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.location }}</a>
              <div class="description">
                {% if devicegraphiccard.location_url != '' %}
                    <a href="{{ devicegraphiccard.location_url }}">{{ devicegraphiccard.location }}</a>
                {% else %}
                    {{ devicegraphiccard.location }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.status }}</a>
              <div class="description">
                {% if devicegraphiccard.state_url != '' %}
                    <a href="{{ devicegraphiccard.state_url }}">{{ devicegraphiccard.state }}</a>
                {% else %}
                    {{ devicegraphiccard.state }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.busID }}</a>
              <div class="description">{{ devicegraphiccard.busID }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.documents }}</a>
              {% for document in devicegraphiccard.documents %}
                <div class="description">
                  {% if document.url != '' %}
                      <a href="{{ document.url }}">{{ document.name }}</a>
                  {% else %}
                      {{ document.name }}
                  {% endif %}
                </div>
              {% endfor %}
            </div>
          </div>
        </div>
      </div>
    </div>
  {% endfor %}

  {% for devicedrive in data.devicedrives|sort((a, b) => a.devicedrive.name|lower <=> b.devicedrive.name|lower) %}
    <div class="ui {{ devicedrive.color }} card">
      <div class="content">
        <div class="header">{{ devicedrive.name }}</div>
        <div class="meta"><span class="ui {{ devicedrive.color }} text">{{ translation.devicedrive }}</span></div>
      </div>
      <div class="content">
        <div class="ui relaxed list">
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.manufacturer }}</a>
              <div class="description">
                {% if devicedrive.manufacturer_url != '' %}
                    <a href="{{ devicedrive.manufacturer_url }}">{{ devicedrive.manufacturer }}</a>
                {% else %}
                    {{ devicedrive.manufacturer }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.write }}</a>
              <div class="description">
                {% if devicedrive.write %}
                  <div style="box-shadow: 0 0 0 #a3c293 inset;background: #fcfff5;color: #2c662d;">
                    <i class="check circle outline icon"></i>
                    {{ devicedrive.write_val }}
                  </div>
                {% else %}
                  <div style="box-shadow: 0 0 0 #e0b4b4 inset;background: #fff6f6;color: #9f3a38;">
                    <i class="times circle outline icon"></i>
                    {{ devicedrive.write_val }}
                  </div>
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.speed }}</a>
              <div class="description">{{ devicedrive.speed }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.interface }}</a>
              <div class="description">
                {% if devicedrive.interface_url != '' %}
                    <a href="{{ devicedrive.interface_url }}">{{ devicedrive.interface }}</a>
                {% else %}
                    {{ devicedrive.interface }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.serial }}</a>
              <div class="description">{{ devicedrive.serial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.inventaire_number }}</a>
              <div class="description">{{ devicedrive.otherserial }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.location }}</a>
              <div class="description">
                {% if devicedrive.location_url != '' %}
                    <a href="{{ devicedrive.location_url }}">{{ devicedrive.location }}</a>
                {% else %}
                    {{ devicedrive.location }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.status }}</a>
              <div class="description">
                {% if devicedrive.state_url != '' %}
                    <a href="{{ devicedrive.state_url }}">{{ devicedrive.state }}</a>
                {% else %}
                    {{ devicedrive.state }}
                {% endif %}
              </div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.busID }}</a>
              <div class="description">{{ devicedrive.busID }}</div>
            </div>
          </div>
          <div class="item">
            <div class="content">
              <a class="header">{{ translation.documents }}</a>
              {% for document in devicedrive.documents %}
                <div class="description">
                  {% if document.url != '' %}
                      <a href="{{ document.url }}">{{ document.name }}</a>
                  {% else %}
                      {{ document.name }}
                  {% endif %}
                </div>
              {% endfor %}
            </div>
          </div>
        </div>
      </div>
    </div>
  {% endfor %}
</div>

{{ include('footer.html.twig') }}
